import 'package:flutter/material.dart';

class TeamManagementLayout extends StatelessWidget {
  const TeamManagementLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('sup'));
  }
}
